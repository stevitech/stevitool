import { useState } from "react";
import { Link } from "react-router-dom";
import { Menu } from "lucide-react";

export default function Navbar({ onToggleSidebar }) {
    const [isDropdownOpen, setIsDropdownOpen] = useState(false);

    return (
        <nav className="w-full px-10 py-4 flex items-center justify-between bg-[hsl(var(--color-glass))] backdrop-blur-md text-[hsl(var(--color-text))] shadow-md fixed top-0 z-50">
            {/* Logo */}
            <Link to="/" className="text-xl font-semibold cursor-pointer">
                Stevi<span className="text-[hsl(var(--color-primary))]">Tools</span>
            </Link>

            {/* Desktop Nav */}
            <div className="hidden md:flex items-center gap-8 text-sm font-medium">
                <Link
                    to="/"
                    className="cursor-pointer hover:text-[hsl(var(--color-primary))]"
                >
                    Home
                </Link>

                {/* Dropdown */}
                <div
                    className="relative group"
                    onMouseEnter={() => setIsDropdownOpen(true)}
                    onMouseLeave={() => setIsDropdownOpen(false)}
                >
                    <div className="flex items-center gap-1 cursor-pointer">
                        <span className="hover:text-[hsl(var(--color-primary))]">
                            Services
                        </span>
                        <svg
                            className={`w-4 h-4 transition-transform duration-200 ${
                                isDropdownOpen ? "rotate-180" : ""
                            }`}
                            fill="currentColor"
                            viewBox="0 0 20 20"
                        >
                            <path
                                fillRule="evenodd"
                                d="M10 14l-6-6h12l-6 6z"
                                clipRule="evenodd"
                            />
                        </svg>
                    </div>

                    {/* Dropdown Panel */}
                    <div
                        className={`absolute left-0 mt-2 w-36 rounded-md shadow-lg bg-[hsl(var(--color-glass))] backdrop-blur-md text-[hsl(var(--color-text))] transition-all duration-300 animate__animated ${
                            isDropdownOpen
                                ? "block animate__fadeInDown"
                                : "animate__fadeOutUp pointer-events-none"
                        }`}
                    >
                        <Link
                            to="/services/link1"
                            className="block px-4 py-2 hover:bg-[hsl(var(--color-muted)/0.2)] cursor-pointer"
                        >
                            Link 1
                        </Link>
                        <Link
                            to="/services/link2"
                            className="block px-4 py-2 hover:bg-[hsl(var(--color-muted)/0.2)] cursor-pointer"
                        >
                            Link 2
                        </Link>
                        <Link
                            to="/services/link3"
                            className="block px-4 py-2 hover:bg-[hsl(var(--color-muted)/0.2)] cursor-pointer"
                        >
                            Link 3
                        </Link>
                    </div>
                </div>

                <Link
                    to="/contact"
                    className="cursor-pointer hover:text-[hsl(var(--color-primary))]"
                >
                    Contact
                </Link>
            </div>

            {/* Mobile Button — ONLY if you want it */}
            {/* <button
                className="md:hidden p-2 rounded hover:bg-[hsl(var(--color-muted)/0.1)] transition"
                onClick={onToggleSidebar}
                aria-label="Open sidebar"
            >
                <Menu className="w-6 h-6 text-[hsl(var(--color-text))]" />
            </button> */}
        </nav>
    );
}

{
    /* Sidebar */
}
<aside
    ref={sidebarRef}
    className={cn(
        "fixed top-0 right-0 h-full w-64 z-50 p-6 pt-16 space-y-6",
        "bg-white dark:bg-neutral-900 text-[var(--color-text)]",
        "shadow-xl transition-transform duration-300 ease-in-out",
        open ? "translate-x-0" : "translate-x-full"
    )}
>
    <nav className="flex flex-col gap-4">
        <Link
            to="/"
            className="text-sm font-medium hover:text-[hsl(var(--color-primary))] transition-colors cursor-pointer"
        >
            Home
        </Link>

        <Accordion title="Services">
            <div className="flex flex-col gap-2">
                <Link
                    to="/services/link1"
                    className="text-sm pl-2 py-1 hover:text-[hsl(var(--color-primary))] transition-colors cursor-pointer"
                >
                    Link 1
                </Link>
                <Link
                    to="/services/link2"
                    className="text-sm pl-2 py-1 hover:text-[hsl(var(--color-primary))] transition-colors cursor-pointer"
                >
                    Link 2
                </Link>
                <Link
                    to="/services/link3"
                    className="text-sm pl-2 py-1 hover:text-[hsl(var(--color-primary))] transition-colors cursor-pointer"
                >
                    Link 3
                </Link>
            </div>
        </Accordion>

        <Link
            to="/contact"
            className="text-sm font-medium hover:text-[hsl(var(--color-primary))] transition-colors cursor-pointer"
        >
            Contact
        </Link>
    </nav>
</aside>;
